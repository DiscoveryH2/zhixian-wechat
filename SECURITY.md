# 安全说明

知弦 PC 可以读取用户打开的微信窗口、用户主动导入的 ChatLab 记录，以及已启用的 WeFlow 本机 API。选定文字、背景和显式处理的媒体可能发送到配置的模型服务。维护重点包括聊天数据和密钥泄露、错误目标回填、导入文件与媒体路径边界、后台任务退出和发行包数据隔离。

## 报告安全问题

请优先通过仓库的 [私密安全报告入口](https://github.com/DiscoveryH2/zhixian-wechat/security/advisories/new) 联系维护者。如果该入口尚未启用，请先在普通 Issue 中仅说明“需要私下报告安全问题”，等待维护者提供私密渠道；不要公开可利用细节、真实聊天或凭据。

报告中可以包含：

- 受影响版本和操作系统。
- 使用合成对话和测试配置的最小复现步骤。
- 预期边界、实际行为，以及可能影响的范围。
- 不含密钥、联系人、聊天正文和私人文件路径的日志摘要。

请不要为验证问题而访问他人数据、向真实联系人发送测试内容或公开他人的信息。维护者收到报告后会确认影响并讨论修复与披露安排；目前不承诺固定响应时限。

## 保护边界

- 窗口 OCR 截图只在内存中处理，不自动发送给视觉模型；文字分析会发送选定消息与匹配背景。
- 显式图片理解和联网语音转写会发送所选媒体字节。朋友圈分析可能处理可用的首张配图；本地语音转写不等于后续文字分析也离线。
- Windows DPAPI 加密保存 Key 和 Token，但不能保护已被攻陷的当前用户账户；笔记、联系人、可选实时历史以及导入的 SQLite 归档均未加密。
- 主动导入仅处理用户选择的文件或目录。媒体引用必须留在对应导出目录边界内；文件缺失、路径越界或无法确认格式时应拒绝，不根据占位文本猜测媒体内容。
- 手动回填只粘贴。可选自动回复只对明确列入本机白名单的新收到纯文字消息工作；发送前重新核验当前可见窗口、会话、最新来信、空且聚焦的输入框和草稿内容。UI 自动化仍可能受到微信布局变化、OCR 错误或竞争状态影响，按键成功不代表送达。
- 自动回复默认每次启动关闭，并受冷却、每小时/每日上限和紧急停止约束。群聊默认只允许可靠确认提及用户的事件；窗口 OCR 无法可靠识别提及，需明确切换到“全部群消息”模式。它不会主动发起对话或群发。
- 自动回复配置 `data/auto-reply.json` 是本机明文，其中的会话标题、ID 和白名单属于私有数据；不要公开该文件。笔记不会用于自动回复草稿。自动回复分析会把当前选定的消息上下文发送到已配置模型服务。
- WeFlow 适配仅连接本机 loopback，拒绝重定向；自动探测只访问健康检查。媒体仅接受数据层已签发的资产引用与允许的本机端点，不代理前端任意 URL。
- 没有运行中的 WeFlow 或主动导入时，只能显示应用已收集的数据，不能宣称已访问全部微信。零散朋友圈媒体缓存不构成帖子索引。
- 模型不能验证他人的隐藏意图。朋友圈判断可参考选定私聊背景，但公开评论草稿生成步骤不接收私聊全文；用户仍需核对内容是否适合公开。
- 发行包不得携带个人 `data/`、SQLite 索引及辅助文件、导出原件、媒体、密钥、真实聊天或私人笔记。

## 归档与删除

导入后会生成 `data/chatlab-index.sqlite3`，其中包含会话、消息、来源位置和媒体关联；SQLite 运行期间还可能产生 WAL / SHM 辅助文件。它们都是私有数据，不因 Key 已加密而得到加密保护。

实时历史保存默认关闭，但主动导入本身就是明确的本地保存操作。关闭实时历史保存或使用当前“清空历史”不会删除导入索引。需要清除归档时，先退出应用，再删除索引及相关辅助文件；原始导出、媒体、系统备份和模型服务日志需要分别处理。文件删除不是安全擦除。

## 验证与披露范围

请分别报告合成夹具、模拟 API、真实微信窗口和真实服务的验证结果。私聊、帖子、导出文件和媒体内容都应按不可信输入处理，不能成为改变模型系统规则或应用操作权限的指令。自动发送不是保证送达的事务机制；状态不明时应停止并人工检查。TTS、语音复刻以及自动点赞和评论不是 v1.3 的已实现能力。

详细数据流见 [隐私说明](docs/PRIVACY.md)。请优先在当前最新发行版复现问题；历史版本暂不承诺长期维护。
