# 安全说明

知弦 PC 会读取用户打开的微信窗口，并可将选定文字发送到用户配置的模型服务。维护重点包括聊天数据和密钥泄露、错误目标回填、后台任务退出、第三方接口边界和发行包数据隔离。

## 报告安全问题

请优先通过仓库的 [私密安全报告入口](https://github.com/DiscoveryH2/zhixian-wechat/security/advisories/new) 联系维护者。如果该入口尚未启用，请先在普通 Issue 中仅说明“需要私下报告安全问题”，等待维护者提供私密渠道；不要公开可利用细节、真实聊天或凭据。

报告中可以包含：

- 受影响版本和操作系统。
- 使用合成对话和测试配置的最小复现步骤。
- 预期边界、实际行为，以及可能影响的范围。
- 不含密钥、联系人、聊天正文和私人文件路径的日志摘要。

请不要为验证问题而访问他人数据、向真实联系人发送测试内容或公开他人的信息。维护者收到报告后会确认影响并讨论修复与披露安排；目前不承诺固定响应时限。

## 保护边界

- 本地截图只在内存中处理；分析使用的文字和匹配背景会发送到用户配置的 API。
- Windows DPAPI 加密保存 Key 和 Token，但不能保护已被攻陷的当前用户账户；笔记、联系人和可选历史不加密。
- 回填需要重新核验窗口与会话，只粘贴，不发送。OCR、同名联系人和界面变化仍可能造成错误，用户必须检查收件人及内容。
- WeFlow 适配仅连接本机 loopback，自动探测只访问健康检查。
- 发行包不得携带个人 `data/`、密钥、真实聊天或私人笔记。

详细数据流见 [隐私说明](docs/PRIVACY.md)。请优先在当前最新发行版复现问题；历史版本暂不承诺长期维护。
